<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
  <% include partials/_style %>
</head>
<body>
    <% include partials/_header %>
    <div class="main">
      <h2>Urls!</h2>
      <ul>
        <% for(url in urls){ %>
          <li><%= url %> - <%= urls[url] %></li>
        <% } %>
      </ul>
      <a href="/urls/new" >Add new Link</a>
    </div>
</body>
</html>
