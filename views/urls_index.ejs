<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
  <% include partials/_style %>
</head>
<body>
    <% include partials/_header %>
    <div class="main">
      <h2>Here's everything in the database</h2>
      <ul class="url-list">
        <% for(url in urls){ %>
          <li><a href="../urls/<%= url %>"><%= url %></a> - <a href="<%= urls[url] %>"><%= urls[url] %></a></li>
        <% } %>
      </ul>
    </div>
</body>
</html>
