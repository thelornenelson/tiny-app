<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <h1>Urls!</h1>
    <ul>
      <% for(url in urls){ %>
        <li><%= url %> - <%= urls[url] %></li>
      <% } %>
    </ul>
</body>
</html>
